import {Router} from "express";

const router: Router = Router();


router.get("/")
router.post("/")
router.put("/")
router.delete("/")

router.post("/login")
router.post("/register")

export default router;