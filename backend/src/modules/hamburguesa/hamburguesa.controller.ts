import type { Request, Response } from "express";

export const getHamburguesas = (req: Request, res: Response) => {
    try {
        
    } catch (error) {
        
    }
};

export const createHamburguesa = (req: Request, res: Response) => {};
